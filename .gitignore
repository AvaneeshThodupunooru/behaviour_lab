# --- never commit ---
.chrome-audit/
.chrome-debug/
__pycache__/
*.pyc
.venv/
_tmp/
node_modules/
dist/
*.log

# runtime session backups (MemoryStore crash recovery)
data/

# secrets / local config (placeholders live in .env.example)
.env
.env.*
!.env.example

# generated report output — several MB per PDF, and every one of them is
# reproducible with `node tools/render_report_pdf.js <SESSION-ID>`
samples/

# editor / agent scratch config
.claude/

# OS junk
Thumbs.db
Desktop.ini
